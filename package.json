{
	"name": "PreceptoriaV2",
	"version": "1.0.0",
	"description": "A comprehensive preceptorship management system",
	"private": true,
	"scripts": {
		"dev": "bun scripts/run dev",
		"build": "bun scripts/run build",
		"test": "bun scripts/run test",
		"typecheck": "bun scripts/run typecheck",
		"test:ui": "cd apps/web && bun run test",
		"test:e2e": "bun scripts/run test:e2e",
		"test:unit": "bun scripts/run test:unit",
		"lint": "bun scripts/run lint",
		"format": "bun scripts/run format",
		"docker": "bun scripts/run docker"
	},
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"devDependencies": {
		"concurrently": "^9.2.0",
		"typescript": "^5.8.3",
		"@types/bun": "^1.2.11"
	},
	"engines": {
		"bun": ">=1.2.0"
	}
}
