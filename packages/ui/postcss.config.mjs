// FIXME: THIS FILE COMES FROM THE SHADCN REPO IT MUST COMPLY TO PREVIOUS STANDARDS
// Also can this be a Typescript file too?
/** @type {import('postcss-load-config').Config} */
const config = {
	plugins: { "@tailwindcss/postcss": {} },
};

export default config;
